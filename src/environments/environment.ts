// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
   firebase : {
    apiKey: "AIzaSyDK8GnjScdSDcwiWrymdonTTihPCR8FF8Y",
    authDomain: "resume-de059.firebaseapp.com",
    databaseURL: "https://resume-de059.firebaseio.com",
    projectId: "resume-de059",
    storageBucket: "resume-de059.appspot.com",
    messagingSenderId: "319422893212",
    appId: "1:319422893212:web:9e4e8d7b857a2f6c"
  }
  // Initialize Firebase

};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
